{
    "name": "boloforms-assignment",
    "version": "1.0.0",
    "description": "Root script to run client and server",
    "scripts": {
        "install:all": "npm install && npm install --prefix client && npm install --prefix server",
        "dev": "npx concurrently \"npm run dev --prefix client\" \"npm run dev --prefix server\""
    },
    "dependencies": {
        "concurrently": "^9.1.0",
        "serverless-http": "^3.2.0",
        "cors": "^2.8.5",
        "dotenv": "^17.2.3",
        "express": "^5.2.1",
        "mongoose": "^9.0.1",
        "multer": "^2.0.2",
        "pdf-lib": "^1.17.1"
    }
}